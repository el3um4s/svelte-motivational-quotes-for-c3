// let backgroundColor = 'red';

// chrome.runtime.onInstalled.addListener(() => {
//   chrome.storage.sync.set({ backgroundColor });
//   console.log('Default background color set to ', `color: ${backgroundColor}`);
// });